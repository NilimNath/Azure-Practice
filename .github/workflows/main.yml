name: TestComplete test

on:
  push:
    branches: 
      - main

jobs:
  test:
    #runs-on: 192.168.1.126
    runs-on: EC2AMAZ-25V74AM
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
  
      - name: Run TestComplete
        run: |
          echo "Running TestComplete"
          ssh Administrator@172.31.30.178 .\runner.bat
