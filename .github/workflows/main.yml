name: TestComplete

on:
  push:
    branches: 
      - main

jobs:
  test:
    runs-on: 192.168.1.126
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
  
      - name: Run TestComplete
        run: |
          echo "Running TestComplete"
          ssh ad\nilim.nath@192.168.1.126 .\runner.bat
